apiVersion: v1
kind: ConfigMap
metadata:
  name: cash-add-on-1-config
  namespace: staging
  labels:
    app: cash
    add-on: add-on-1
    cluster: cluster-1
    environment: staging
data:
  platform: "cash"
  add_on: "add-on-1"
  cluster: "cluster-1"
  environment: "staging"
